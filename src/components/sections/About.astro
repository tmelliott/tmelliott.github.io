---
import { Image } from "astro:assets";
import profileImage from "../../img/tomelliott.jpg";

const titles = ["Data scientist", "Statistical software developer"];
---

<div
  class="max-w-4xl text-blue-50 w-full flex flex-col lg:flex-row items-center justify-center px-4 gap-24 my-24"
>
  <div class="h-64 w-64 rounded relative border-2 border-purple-200">
    <Image
      src={profileImage}
      alt="Tom Elliott"
      class="rounded object-cover w-full h-full z-10 absolute inset-0 object-left hover:scale-105"
    />
  </div>
  <div class="flex-1 flex flex-col items-center lg:items-start">
    <h1 class="text-4xl text-purple-300">
      <strong>Kia ora</strong> &mdash; I'm Tom
    </h1>
    <h2
      class="flex flex-col sm:flex-row sm:gap-2 text-xl font-extralight items-center my-2 sm:my-0"
    >
      {
        titles.map((title, i) => (
          <div>
            <div class="hidden sm:inline">{i > 0 && "|"}</div> {title}
          </div>
        ))
      }
    </h2>

    <p class="mt-8 text-center lg:text-left">
      Lorem ipsum dolor sit, amet consectetur adipisicing elit. Deleniti animi
      similique blanditiis reprehenderit aliquam? Suscipit iure consequuntur
      sequi, illo, temporibus quasi veniam voluptatem repellat, exercitationem
      cupiditate quisquam minus neque aliquid.
    </p>
  </div>
</div>
